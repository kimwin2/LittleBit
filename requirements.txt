# ==============================================================================
# LittleBit - Python Dependencies
# ==============================================================================
# Install PyTorch first (see setup_env.sh), then: pip install -r requirements.txt

# --- Core ML ---
numpy>=2.2.4
transformers>=4.53.0
accelerate>=1.9.0
datasets>=4.0.0
safetensors>=0.5.3
tqdm

# --- Training ---
deepspeed>=0.17.2
tensorboardX
pytorch-optimizer>=3.5.0
peft>=0.16.0
bitsandbytes>=0.46.0
einops

# --- Evaluation ---
lm-eval==0.4.9
sentencepiece

# --- Utilities ---
GPUtil>=1.4.0
mpi4py

# --- C++ Kernel Build ---
pybind11
ninja
